<UserControl x:Class="Galileo.Chat.Client.Gui.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Border Width="380" VerticalAlignment="Center" HorizontalAlignment="Center"
                Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1" CornerRadius="10" Padding="28,26">
            <StackPanel>
                <TextBlock Text="Entrar" Style="{StaticResource HeaderText}" />
                <TextBlock Text="Use sua conta ou registre uma nova."
                           Style="{StaticResource MutedText}" Margin="0,4,0,20" />

                <TextBlock Text="Usuário" Margin="0,0,0,4" />
                <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Text="Senha" Margin="0,14,0,4" />
                <PasswordBox x:Name="PasswordInput" />

                <TextBlock Text="Apelido (só para registro)" Margin="0,14,0,4"
                           Style="{StaticResource MutedText}" />
                <TextBox Text="{Binding Nickname, UpdateSourceTrigger=PropertyChanged}" />

                <Grid Margin="0,20,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="10" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Content="Registrar" Command="{Binding RegisterCommand}" />
                    <Button Grid.Column="2" Content="Entrar" Style="{StaticResource AccentButton}"
                            Command="{Binding LoginCommand}" IsDefault="True" />
                </Grid>

                <TextBlock Text="{Binding Status}" Margin="0,16,0,0" TextWrapping="Wrap">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                            <Setter Property="Foreground" Value="{StaticResource MutedBrush}" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsError}" Value="True">
                                    <Setter Property="Foreground" Value="{StaticResource ErrorBrush}" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
